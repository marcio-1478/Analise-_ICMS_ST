# Copie para secrets.toml (deploy) ou use .env (local)
# NUNCA commite o arquivo secrets.toml com valores reais

# Supabase (obrigatório para deploy)
SUPABASE_URL = "https://seu-projeto.supabase.co"
SUPABASE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Alternativa: estrutura aninhada
# [supabase]
# url = "https://seu-projeto.supabase.co"
# key = "eyJ..."

# Auth (opcional - se usar tabela usuarios no Supabase, não precisa)
# [auth]
# salt = "st_analyzer_salt"
# [auth.users]
# admin = "hash_sha256_da_senha"
